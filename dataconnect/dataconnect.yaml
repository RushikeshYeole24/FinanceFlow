specVersion: "v1beta"
serviceId: "finance-management-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "finance-management-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
